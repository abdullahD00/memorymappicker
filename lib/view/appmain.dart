import 'package:flutter/material.dart';

class AppMainView extends StatelessWidget {
  const AppMainView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("adasdas");
  }
}
